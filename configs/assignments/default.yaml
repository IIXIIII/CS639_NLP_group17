import: definition.yaml

concurrency:
  task:
    os-std: 5
  agent:
    gpt-3.5-turbo-0613: 5

assignments:
  - agent:
      - gpt-3.5-turbo-0613
    task:
      - os-std

output: "outputs/{TIMESTAMP}"
